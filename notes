Tests
	- 

Make a list of objects, type of which can be sphere, plane, cube, cylinder, cone
	- function pointers D:
For every pixel on the screen loop through every? object in the scene and ask if this ray intersects
	- statically allocated # of objects maybe, if over max, error out
Movable camera
	- ???
lerp 
	blendedValue = (1 − 𝑡) ⋅ startValue + 𝑡 ⋅ endValue